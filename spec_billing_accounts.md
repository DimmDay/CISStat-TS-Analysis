# Спецификация: Account / Seats (моно/мульти доступ) + Compute Unit метеринг

**Задача:** Task 102 (продолжение worklog2.md)
**Синхронизация:** `main` @ `a42df0a75a022336daa85862cae041633dd4ac38`
**Статус:** Архитектурный дизайн, готов к ревью тимлидом перед стадией TDD-реализации
**Автор:** Claude (Senior dev), платформа CISStat TS Analysis
**Расширяет:** `ROLES_AND_PLANS_SPEC.md` (контракт Role/Plan/Capability) — не заменяет, а достраивает недостающий уровень «кто платит и сколько»
**Затрагиваемые области:** `apps/api/plans.py`, `apps/api/auth.py`, новый модуль метеринга `apps/api/billing/`, `packages/ui/lib/plans.ts`, роутеры, требующие `require_capability`

---

## 1. Контекст и цель

`ROLES_AND_PLANS_SPEC.md` закрывает вопрос «кто/что можно/за что заплатили»
на уровне одного принципала (`AuthenticatedPrincipal`), но не отвечает на
два требования продукта:

1. Платформа самообслуживания должна поддерживать **моно-доступ** (один
   человек = один аккаунт = один тариф) и **мульти-доступ** (команда из N
   персон, тариф автоматически пересчитывается от их числа) —
   назначается самим пользователем, без участия отдела продаж.
2. Учёт потребления должен вестись в единице, которая честно отражает
   реальную стоимость вычислений платформы (а не токенов текста) —
   **Compute Unit (CU)** — и закрывает уже признанный в `ROLES_AND_PLANS_SPEC.md`
   пробел п.7.2 («лимиты объявлены, но нигде не инкрементируются»).

Эта спецификация вводит недостающий уровень сущностей **выше**
`AuthenticatedPrincipal` — `Account` — и модуль метеринга `Compute Unit`,
не переписывая уже принятый контракт `Role`/`Plan`/`Capabilities`, а
подключая его к новому уровню биллинга.

## 2. Модель данных

### 2.1 Новая сущность: `Account`

```python
# apps/api/plans.py (расширение)

class AccountType(str, Enum):
    MONO = "mono"    # один человек, seats всегда 1, не редактируется
    MULTI = "multi"  # команда, seats назначается владельцем аккаунта

class AccountRole(str, Enum):
    """Права ВНУТРИ аккаунта. Отдельная ось от Role (кто) и Plan (что можно).
    Не путать AccountRole.OWNER с платформенной Role.ADMIN — это разные оси."""
    OWNER = "owner"    # управляет составом seats и биллингом
    MEMBER = "member"  # использует возможности плана, не управляет биллингом

class Account(BaseModel):
    id: UUID
    account_type: AccountType
    plan: PlanName                      # тариф назначен АККАУНТУ, не принципалу
    seats: int = Field(ge=1)            # для MONO жёстко зафиксировано в 1
    plan_expires_at: datetime | None
    billing_cycle_anchor: datetime
    compute_unit_pool_balance: float    # текущий остаток пула CU на цикл
    compute_unit_pool_reset_at: datetime

class AccountMembership(BaseModel):
    account_id: UUID
    principal_id: UUID
    account_role: AccountRole
    joined_at: datetime
```

`AuthenticatedPrincipal` из `ROLES_AND_PLANS_SPEC.md` не удаляется и не
переписывается концептуально — он приобретает ссылку на аккаунт:

```python
class AuthenticatedPrincipal(BaseModel):
    id: UUID
    role: Role                    # без изменений — кто
    account_id: UUID              # НОВОЕ — ссылка на биллинговый аккаунт
    account_role: AccountRole     # НОВОЕ — права внутри аккаунта
    capabilities: Capabilities    # без изменений по форме, но теперь
                                   # резолвится через account.plan,
                                   # а не через principal.plan напрямую
```

Важно: поле `plan` **уходит с уровня принципала на уровень аккаунта**.
`Capabilities` для конкретного запроса резолвятся как
`PLAN_DEFINITIONS[account.plan]` — сам паттерн `require_capability(...)`
из `auth.py` не меняется ни на строчку, меняется только то, откуда
берётся план при резолве принципала (`get_current_principal` подтягивает
`account_id` → `account.plan`, вместо `principal.plan` напрямую). Это
намеренно минимальная точка изменения — весь остальной код платформы,
уже написанный против `Capabilities`, не требует правок.

### 2.2 `internal_analyst`/`admin` и Account

Для `embedded`-режима (`admin`, `internal_analyst`) вопрос моно/мульти
не имеет смысла — им, как и сегодня, назначается служебный Account с
полным доступом без биллинга (`plan` не применяется, как и в исходном
документе). Явное решение: завести отдельный псевдо-план
`PlanName.INTERNAL` без цены и без CU-лимитов (`compute_unit_pool_balance`
не расходуется/не проверяется для такого Account), чтобы не городить
условную логику «если admin — пропустить биллинг» по всему коду, а
остаться в рамках того же единого механизма резолва через
`Capabilities`.

## 3. Моно / Мульти доступ

### 3.1 Выбор пользователем при регистрации/апгрейде

Self-service флоу:
1. Пользователь регистрируется → создаётся `Account(account_type=MONO, seats=1)`,
   он же становится `AccountMembership(account_role=OWNER)`.
2. В любой момент из настроек аккаунта пользователь может переключить
   `account_type → MULTI` — это **не создаёт новый Account**, а
   переводит существующий (сохраняя историю анализов/сессий,
   привязанных к `account_id`, а не к отдельному принципалу — важное
   архитектурное следствие: история анализов **уже должна быть**
   собственностью Account, а не Principal, иначе при переходе moно→мульти
   данные владельца «потеряются» для остальных участников команды).
3. В режиме MULTI владелец (`AccountRole.OWNER`) приглашает
   участников по email — каждое принятое приглашение создаёт нового
   `Principal` + `AccountMembership(account_role=MEMBER)` и увеличивает
   `Account.seats` на 1.
4. Удаление участника — `seats -= 1`, немедленно.

### 3.2 Автоматический расчёт цены

Две **независимые** формулы, которые нельзя смешивать:

- **Цена** — определяется числом seats, не фактическим потреблением:
  ```
  price(account) = PLAN_DEFINITIONS[account.plan].base_price × account.seats
  ```
  Линейная модель — намеренный выбор для v1: пользователь может
  посчитать свою цену в уме до того, как начал пользоваться платформой,
  без обращения в поддержку. Ступенчатые объёмные скидки (стандартный
  enterprise-паттерн) — явный follow-up (§9), реализуется как
  отдельная таблица `SEAT_PRICING_TIERS`, не меняющая эту формулу, а
  подменяющая множитель.
- **Квота потребления (CU)** — пулится на уровне Account и
  масштабируется от seats, а не делится поровну на участника:
  ```
  compute_unit_pool_per_cycle(account) = PLAN_DEFINITIONS[account.plan].cu_per_seat × account.seats
  ```
  Обоснование пулинга, а не индивидуальных квот на участника — в
  реальной команде нагрузка асимметрична (один аналитик гоняет
  tuning/backtest, остальные просматривают Обзоры) — индивидуальные
  квоты гарантированно создают ситуацию «моя квота кончилась, хотя я её
  не тратил», что противоречит идее самообслуживания без ручного
  вмешательства поддержки.

### 3.3 Пересчёт при изменении состава

Изменение `seats` (добавление/удаление участника) должно:
- немедленно обновить `compute_unit_pool_per_cycle` (пропорционально,
  без ожидания следующего расчётного периода — иначе владелец,
  добавивший участника, не получит немедленно доступную для команды
  квоту);
- передать новое значение `quantity` в биллинг-провайдера (см. §7) для
  proration — списание/зачисление разницы стоимости за оставшийся
  период текущего цикла, а не постфактум в конце месяца.

## 4. Compute Unit (CU) — единица учёта

### 4.1 Почему не токены (кратко, полная аргументация — в обсуждении, зафиксирован вывод)

Платформа генерирует результаты гетерогенных численных алгоритмов, чья
стоимость на одинаковом объёме входных данных различается на порядки
(простая статистика vs CWT vs PELT vs backtest/tuning с комбинаторным
ростом от `trials × folds`). Единица «токен» у LLM работает, потому что
предельная стоимость обработки токена в фиксированной архитектуре
модели примерно постоянна — у нашей платформы такой однородности
физически нет, поэтому буквальный перенос «input/output tokens»
систематически исказил бы цену: либо демпинг тяжёлых операций, либо
переплата за тривиальные.

Что переносится из LLM-биллинга **по паттерну**, а не по единице
измерения:
- непрерывный метеринг с балансом/списанием вместо грубых лимитов;
- дифференциация цены **по типу операции** (у LLM — по модели, здесь —
  по алгоритму), а не единая ставка на всё;
- раздельный учёт «подготовки данных» (обычно дешевле) и «тяжёлого
  расчёта» (обычно дороже) как отдельных компонент формулы, а не как
  токенов символьного потока.

### 4.2 Формула

```python
# apps/api/billing/compute_units.py

def compute_cu(
    operation: OperationType,
    rows: int,
    columns: int,
    repetitions: int = 1,   # folds × trials для tuning/backtest, иначе 1
) -> float:
    coef = ALGORITHM_COEFFICIENTS[operation]  # калибруется эмпирически
    data_volume = rows * columns
    return coef.base * data_volume ** coef.exponent * repetitions
```

`ALGORITHM_COEFFICIENTS` — таблица по типу операции, источник истины
по аналогии с `PLAN_DEFINITIONS`:

| Операция (`OperationType`) | `base` | `exponent` | Комментарий |
|---|---|---|---|
| `basic_stats` / `smoothing` | низкий | ~1.0 | линейно от объёма |
| `stl_decomposition` | средний | ~1.0–1.1 | |
| `spectral_periodogram` | средний | ~1.1 | |
| `cwt` | высокий | ~1.2–1.3 | масштабы CWT учитываются как множитель в `repetitions`, не в `columns` |
| `structural_breaks_pelt` | высокий, зависит от бюджета сетки | ~1.2 | переиспользует существующий бюджет-параметр PELT (см. §4.3) |
| `model_backtest` | высокий | ~1.0 | `repetitions = folds` |
| `model_tuning` | самый высокий | ~1.0 | `repetitions = trials × folds` |

Конкретные числовые значения `base`/`exponent` — **не фиксируются в этой
спецификации** (открытый вопрос §9, требует калибровки по реальным
замерам, не по догадке).

### 4.3 Ключевое архитектурное решение: переиспользование, не новая инфраструктура

Платформа уже располагает инфраструктурой измерения стоимости операций:
- бюджетирование PELT-сетки (фикс HTTP 502 в «Структурных сдвигах»,
  Task 76) — уже связывает объём данных с вычислительным бюджетом;
- диагностика исполнения тюнинга (`TuningPlanExecution`, Task 99) — уже
  фиксирует фактическое число trials/folds на исполнение.

`compute_cu(...)` не дублирует эти измерения, а **читает уже
существующие величины** (бюджет PELT-сетки, число trials/folds из
`TuningPlanExecution`) как входные параметры формулы — это одна и та же
точка данных, используемая сегодня для защиты от таймаута/502 и теперь
дополнительно для метеринга CU. Первая задача Этапа 1 (§10) — аудит
этих двух мест кода, чтобы `compute_cu` не переизобретал их, а
параметризовался ими напрямую.

### 4.4 Где считается и списывается

```python
# apps/api/auth.py — расширение require_capability

def require_capability(capability: str, operation: OperationType | None = None):
    def dependency(principal: AuthenticatedPrincipal = Depends(get_current_principal)):
        if not getattr(principal.capabilities, capability):
            raise HTTPException(403, ...)
        if operation is not None:
            # резервируем CU ДО выполнения тяжёлой операции (не после)
            reserve_compute_units(principal.account_id, operation)
        return principal
    return dependency
```

Принципиальное решение: **CU резервируется до выполнения операции**, а
не списывается по факту после — иначе тяжёлая операция (например,
tuning на грани лимита) успеет отработать и создать реальную
инфраструктурную стоимость даже при исчерпанном пуле, что подрывает
саму цель метеринга (защита пула = защита инфраструктурного бюджета
render.com, не только бухгалтерия). При недостатке пула — `402 Payment
Required`/аналог, а не «пусть досчитается, потом запретим следующий
запрос».

После фактического выполнения — точная фиксация факта (`rows`,
`repetitions` могли отличаться от оценки на входе, например, tuning
завершился раньше по early-stopping) — резерв корректируется до
фактического значения, не обязан совпадать один в один с оценкой.

## 5. Frontend-зеркало (`packages/ui/lib/plans.ts`)

Расширяется теми же сущностями (`Account`, `AccountType`, `AccountRole`,
`compute_unit_pool_balance`) — используется **только для UX** (баннер
«осталось N CU», кнопка приглашения участника видна только `OWNER`,
переключатель моно→мульти в настройках) — реальная проверка баланса и
резервирование остаются исключительно на бэкенде, как и зафиксировано
в исходном документе для `Capabilities` (§5 `ROLES_AND_PLANS_SPEC.md`
про фронтенд-зеркало распространяется и на этот контракт).

## 6. Тестовая стратегия (TDD)

- **`Account`/`AccountMembership` — модельный уровень:**
  - создание `MONO`-аккаунта фиксирует `seats == 1`, попытка изменить
    `seats` для `MONO` — отклоняется на уровне валидации модели;
  - переключение `MONO → MULTI` сохраняет `account_id` (история
    анализов не теряется — тест на то, что все существующие сессии
    остаются доступны после переключения).
- **Ценообразование:**
  - `price(account)` линейно масштабируется от `seats` для каждого
    `PlanName`;
  - изменение `seats` немедленно (без ожидания цикла) обновляет
    `compute_unit_pool_per_cycle` пропорционально.
- **Compute Unit:**
  - `compute_cu(...)` даёт монотонно неубывающий результат при росте
    `rows`/`repetitions` для каждой операции (regression guard от
    случайной поломки коэффициентов);
  - резервирование CU до выполнения операции блокирует запрос при
    недостаточном балансе пула (`402`), **не запуская** тяжёлый расчёт
    (тест проверяет, что сам обработчик операции не вызывается —
    мок/spy на функцию расчёта);
  - фактическая корректировка резерва после выполнения (early-stopping
    tuning) уменьшает списание относительно первоначальной оценки.
- **Пулинг vs индивидуальные квоты:**
  - тест на сценарий «один участник MULTI-аккаунта тратит весь пул —
    остальные участники немедленно получают `402` на следующей
    тяжёлой операции», подтверждающий совместное, а не раздельное
    потребление.
- **Обратная совместимость с `ROLES_AND_PLANS_SPEC.md`:**
  - все существующие тесты `require_capability(...)` из текущей
    реализации продолжают проходить без изменений (только источник
    `plan` меняется с `principal.plan` на `account.plan` — контракт
    самого `Capabilities`-резолва для вызывающего кода идентичен).

## 7. Интеграция с биллинг-провайдером

Вне скоупа этой спецификации (отдельная задача) — но архитектура должна
закладывать точку расширения уже сейчас: изменение `Account.seats`
должно вызывать `sync_subscription_quantity(account_id, seats)` —
абстрактный интерфейс, за которым в дальнейшем встанет конкретный
провайдер (Stripe-подобный, с proration «из коробки»). Пока — заглушка,
логирующая намерение, без реального списания денег, до выбора
провайдера тимлидом.

## 8. Риски и меры

| Риск | Мера |
|---|---|
| Резервирование CU «до» операции занижает/завышает оценку относительно факта | Пост-фактум корректировка резерва (§4.4), калибровка коэффициентов на реальных замерах, не на догадке |
| Общий пул CU даёт одному «прожорливому» участнику монополизировать квоту команды | Осознанный компромисс §3.2 — задокументирован явно, опция индивидуальных суб-лимитов внутри пула — follow-up, не v1 |
| Переход MONO→MULTI неявно теряет историю анализов, если она была привязана к Principal, а не Account | История анализов проектируется как принадлежащая `account_id` с самого начала (§3.1, п.2) — фиксируется как требование к модели сессий анализа, не как то, что можно исправить позже |
| Рассинхронизация `plans.py`/`plans.ts` усугубляется новыми сущностями | Не откладывать генерацию TS-типов из Pydantic (уже отмечено как п.7.4 в `ROLES_AND_PLANS_SPEC.md`) — эта спецификация делает проблему on the ground более острой, приоритет стоит поднять |
| `ALGORITHM_COEFFICIENTS` откалиброваны неверно (занижены/завышены) на старте | Значения не фиксируются в спецификации намеренно (§4.2) — калибровочная задача с реальными бенчмарками, отдельный Этап |

## 9. Открытые вопросы к тимлиду

1. Точные значения `ALGORITHM_COEFFICIENTS` (`base`/`exponent` на
   операцию) — требуется калибровочный бенчмарк на render.com, не
   экспертная оценка.
2. Нужны ли в v1 ступенчатые объёмные скидки (`SEAT_PRICING_TIERS`) или
   линейная модель `price × seats` достаточна на старте (рекомендация
   этой спецификации — начать с линейной, см. §3.2).
3. Выбор биллинг-провайдера (§7) — Stripe или альтернатива, влияет на
   точный контракт `sync_subscription_quantity`.
4. Нужен ли индивидуальный суб-лимит CU на участника **внутри** общего
   пула Account (защита от риска из таблицы §8, если пулинг без
   ограничений окажется проблемой на практике) — предлагается не решать
   заранее, а мониторить после пилота.
5. Что происходит с `AccountMembership` при удалении единственного
   `OWNER` из `MULTI`-аккаунта — нужен явный процесс передачи
   владения, не описанный в этой версии.

## 10. Поэтапный роллаут

1. **Этап 0 (эта спецификация).** Ревью тимлидом, решение по открытым
   вопросам §9, в первую очередь — калибровочный бенчмарк для
   `ALGORITHM_COEFFICIENTS`.
2. **Этап 1 — фундамент модели.** `Account`, `AccountMembership`,
   `AccountType`, `AccountRole` в `plans.py`; миграция
   `AuthenticatedPrincipal` на резолв плана через `account_id`; полный
   TDD-цикл §6 (модельный уровень + ценообразование), без подключения
   реального биллинг-провайдера.
3. **Этап 2 — Compute Unit метеринг.** `compute_cu(...)`, интеграция в
   `require_capability(...)` с резервированием до выполнения; аудит и
   переиспользование бюджета PELT (Task 76) и `TuningPlanExecution`
   (Task 99) как источников входных параметров (§4.3) — не
   переизобретать эти измерения.
4. **Этап 3 — self-service флоу.** UI переключения MONO↔MULTI,
   приглашение участников, `packages/ui/lib/plans.ts` зеркало.
5. **Этап 4 — биллинг-провайдер.** Реальная интеграция `sync_subscription_quantity`
   (§7), proration, отдельная задача по выбору провайдера.

## 11. Манифест файлов (к реализации на Этапе 1–2)

Новые/изменяемые backend-файлы:
- `apps/api/plans.py` — добавление `AccountType`, `AccountRole`,
  `Account`, `AccountMembership`; расширение `AuthenticatedPrincipal`
- `apps/api/auth.py` — резолв плана через `account_id`, расширение
  `require_capability(...)` параметром `operation`
- `apps/api/billing/compute_units.py` — `compute_cu`,
  `ALGORITHM_COEFFICIENTS`, `reserve_compute_units`,
  `settle_compute_units` (пост-фактум корректировка)
- `apps/api/billing/compute_units.test.py`
- `apps/api/plans.test.py` — расширение существующих тестов на
  `Account`/ценообразование/пулинг

Frontend:
- `packages/ui/lib/plans.ts` — зеркало `Account`/`AccountType`/
  `AccountRole`/остаток пула CU

---

*Конец спецификации. Реализация — отдельными TDD-итерациями согласно
AGENTS.md, начиная с Этапа 1 после утверждения тимлидом открытых
вопросов §9. Коммит/push в `main` не выполняется этим агентом —
только подготовка артефакта на скачивание.*
