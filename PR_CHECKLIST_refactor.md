# Чек-лист перед отправкой PR — CISStat TS Analysis

> Заполняется автором PR перед запросом ревью. Если на пункт отвечаешь "нет" или "не применимо" — кратко напиши почему в описании PR. Пустой чек-лист = PR не принимается к ревью.

**PR:** _______________  **Этап рефакторинга:** _______________  **Связанный раздел ARCHITECTURE.md:** _______________

---

## 1. Тесты (Test-Driven Refactoring — железное правило Этапа 3)

- [ ] Для каждого нового файла в `app/core/`, `app/validation/`, `app/features/` (и любого другого модуля бизнес-логики) есть unit-тест
- [ ] Если функция перенесена из legacy (`app.py`) — есть тест, сравнивающий результат с Golden Dataset / Snapshot
- [ ] Тесты на граничные случаи добавлены, если модуль их касается: короткие ряды, 100% пропуски, все выбросы, несходимость модели, частичный сбой Panel
- [ ] Для Panel-кода — есть тест, явно проверяющий отсутствие Data Leakage (groupby по `entity_col`, не плоский shift)
- [ ] Все тесты проходят локально (`pytest`), включая существующие — ничего не сломано

## 2. Соответствие архитектурным инвариантам

Отметь, какие из правил `ARCHITECTURE.md` касаются этого PR, и подтверди соблюдение:

- [ ] **Явная адресация данных (Правило 14):** данные передаются через аргументы функции или `AppState.datasets.{stage}.df` — никакого implicit "контекста вкладки"
- [ ] **`AppState.df` не используется** в новом коде (только в явно помеченных legacy-обёртках)
- [ ] **Data Leakage (Правило 15):** временные признаки на Panel-данных строго через `df.groupby(entity_col).transform/shift(...)`
- [ ] **Атомарность `best_model`/`model_params` (Правило 17):** если PR трогает Modeling — оба поля обновляются в одной точке кода, не раздельно
- [ ] **Dual-Track Modeling:** если PR трогает Model Registry/Experiment Log — привязка идёт через `(source_stage, dataset_version_id)`, без жёстких предположений о `features_version_id`
- [ ] **Разделение `modeling/` / `forecasting/` (Правило 11):** `forecasting/` не содержит подбора гиперпараметров или выбора архитектуры
- [ ] **Кэширование:** в `@st.cache_data` передаётся `version_id` (строка), не сам DataFrame
- [ ] **Graceful Degradation (Правило 16):** критические операции обёрнуты в `try-except`, ошибка залогирована в `AppState.error_log` (stage, severity, error_type, message), нет голого `except: pass`
- [ ] **Каскадная инвалидация:** если PR меняет состояние на этапе N — downstream-поля `AppState` (N+1, N+2…) явно инвалидируются, а не остаются "молчаливо устаревшими"
- [ ] Не применимо ни одно из вышеперечисленных — PR не касается этих зон (объясни, какой именно код меняется)

## 3. Границы модулей и структура

- [ ] Новый код размещён ровно в том модуле `app/...`, что предписан `ARCHITECTURE.md` — без альтернативных путей
- [ ] Бизнес-логика (`core/`, `eda/`, `modeling/`, `validation/` и т.д.) не импортирует `streamlit`
- [ ] UI-код (`ui/tabs/...`) не содержит вычислений — только вызовы бизнес-логики и рендеринг
- [ ] Если добавлен класс — он оправдан состоянием/инвариантом/полиморфизмом (см. системный промпт, раздел "ООП точечно"), а не используется по умолчанию для всего

## 4. Синхронизация документации (Правило 20 — защита от Documentation Drift)

- [ ] Если добавлено новое поле `AppState` — обновлён раздел "Ключевые сущности" в `ARCHITECTURE.md`
- [ ] Если поведение модуля отличается от того, что описано в `ARCHITECTURE.md` — это явно отмечено в описании PR (не исправлено тихо по своей инициативе)
- [ ] Если в процессе работы найдена несогласованность между разделами `ARCHITECTURE.md` — она зафиксирована отдельно (issue/комментарий), а не "решена на месте"

## 5. Обработка ошибок и устойчивость

- [ ] Учтены сценарии из раздела "Обработка ошибок и Graceful Degradation": атомарность этапов (состояние `AppState.datasets.{stage}` не обновляется при сбое), классификация ошибок (critical/warning/info)
- [ ] Сообщения пользователю не содержат технических деталей (traceback, имена внутренних переменных) — только понятный текст

## 6. Самопроверка перед ревью

- [ ] Я бы мог объяснить ревьюеру, какому конкретному разделу/правилу `ARCHITECTURE.md` соответствует каждое нетривиальное решение в этом PR
- [ ] Я перечитал диff целиком (не только финальную версию файлов) — нет случайно оставленного debug-кода, закомментированных блоков, неиспользуемых импортов
- [ ] Изменения соответствуют ровно одному пункту плана Этапов рефакторинга (PR не смешивает несколько этапов без необходимости)

---

### Краткое описание для ревьюера (заполняет автор)

**Что меняется:**


**Какие пункты ARCHITECTURE.md затронуты:**


**Есть ли отступления от инвариантов и почему:**

