# apps/api/requirements-neural.txt
# Task 137 -- Neural Runtime Contract: deploy-манифест dependency-группы
# "neural" (см. apps/api/model_jobs.py::_DEPENDENCY_GROUPS,
# install_extra="neural").
#
# Честность платформы:
# - Task 137: файл НЕ входил в production-сборку apps/api/Dockerfile --
#   все пять нейро-моделей (lstm, nbeats, nhits, tft, deepar) были
#   catalog_only до своих вертикальных срезов.
# - Task 138: ПЕРВЫЙ исполнитель контракта зарегистрирован (lstm:
#   cell ∈ {lstm, gru}), поэтому consistency-gate dispatch<->readiness
#   требует нейро-runtime в production-образе -- файл входит в сборку
#   apps/api/Dockerfile (torch -- CPU-колёса с явным --index-url).
# - Task 139: ВТОРОЙ исполнитель зарегистрирован (nbeats: stack_config
#   ∈ {interpretable, generic}); neural-runtime в образе уже требуется
#   с Task 138.
# - Task 140: ТРЕТИЙ исполнитель зарегистрирован (nhits:
#   interpolation_config ∈ {hierarchical, light}); пара nbeats/nhits --
#   готовая база сравнения на одном runtime.  Остальные две модели
#   (tft, deepar -- Tasks 141-142) остаются catalog_only до своих срезов.
# - Установка локально: pip install -r apps/api/requirements-neural.txt
#   (CPU-вариант torch) либо pip install -e ".[neural]" при появлении extra.
# - Зафиксированная версия neuralforecast связывает surface, на которой
#   сертифицирован единый runtime (проб scripts/task137_neural_api_probe.py):
#   max_epochs deprecated (единый бюджет max_steps), accelerator обязателен
#   явный (BaseModel молча ставит "gpu"), quantile-выходы point-loss моделей
#   через conformal (fit prediction_intervals + predict level).
torch>=2.1,<3                # BaseModel/Lightning-движок; CPU-колёса: --index-url https://download.pytorch.org/whl/cpu
neuralforecast>=3.0,<4.0     # единый runtime пяти нейро-моделей (Task 137)
