# === Основные зависимости ===
streamlit>=1.30.0
# pandas закреплён строго ниже 3.0: app/features/rolling.py::apply_wma использует
# .fillna(method='bfill'), а параметр method был удалён в pandas 3.0.
# Снять верхнюю границу можно только вместе с фиксом rolling.py (см. tools/passport_parity_check.py
# для образца процесса: golden-тест до правки, потом .bfill() вместо fillna(method=...)).
pandas>=2.0.0,<3.0.0
plotly>=5.18.0
sqlalchemy>=2.0.0
clickhouse-connect>=0.7.0
numpy>=1.24.0
scipy>=1.11.0
 
# === Валидация ===
pandera>=0.18.0
missingno>=0.5.2
scikit-learn>=1.3.0
great-expectations>=0.18.0  # опционально, можно подключить позже
pydantic>=2.0.0  # требуется src/catalog/models_catalog.py — отсутствовал, ломал сборку CI
 
# === Временные ряды / статистика ===
matplotlib>=3.7.0
statsmodels>=0.14.0
arch>=7.0.0  # реальный Phillips–Perron; ADF не используется как ложная подмена
ruptures>=1.1.9  # PELT для локализации нескольких структурных сдвигов
PyYAML>=6.0
PyWavelets>=1.4.0
openpyxl
