# === Основные зависимости ===
streamlit>=1.30.0
# Верхняя граница пока сохраняется консервативно до полного pandas 3 parity-прогона.
# Прежний явный блокер app/features/rolling.py::apply_wma устранён в Task 83:
# WMA больше не использует fillna(method=...) / bfill и не подсматривает в будущее.
# Снимать ограничение следует отдельной задачей после полного golden/regression набора.
pandas>=2.0.0,<3.0.0
plotly>=5.18.0
sqlalchemy>=2.0.0
clickhouse-connect>=0.7.0
numpy>=1.24.0
scipy>=1.11.0
 
# === Валидация ===
pandera>=0.18.0
missingno>=0.5.2
scikit-learn>=1.3.0
great-expectations>=0.18.0  # опционально, можно подключить позже
pydantic>=2.0.0  # требуется src/catalog/models_catalog.py — отсутствовал, ломал сборку CI
 
# === Временные ряды / статистика ===
matplotlib>=3.7.0
statsmodels>=0.14.0
arch>=7.0.0  # реальный Phillips–Perron; ADF не используется как ложная подмена
ruptures>=1.1.9  # PELT для локализации нескольких структурных сдвигов
PyYAML>=6.0
PyWavelets>=1.4.0
openpyxl
