# Спецификация (v2): раскрытие/схлопывание вложенных графиков в «Обзоре»

**Задача:** Task 97 (продолжение worklog2.md)
**Статус:** Архитектурный дизайн v2 — на ревью тимлидом перед стадией TDD-реализации
**Автор исходной версии (v1):** Claude (Senior dev), платформа CISStat TS Analysis, 2026-09-03
**Редакция v2:** Claude (Senior dev), 2026-09-06 — внесены правки по результатам ревью против кодовой базы на коммите `039ba7c`
**Затрагиваемые области:** `packages/ui` (общие компоненты, используемые embedded и standalone), все вкладки, где применяется «Обзор» (Validation, Preprocessing, EDA, Modeling), опционально `apps/api` (сэмплирование)

---

## 0. Что изменилось в v2 и почему (changelog ревью)

v1 проверена пофайлово против кодовой базы (`039ba7c`). Часть предпосылок подтвердилась
(масштаб — 30 `*Overview.tsx`; контракт `h-[468px]` — 51 файл; `ResponsiveContainer height="100%"`
— 23 файла; lucide-react и recharts в зависимостях; прецедент статических контрактных тестов;
FastAPI query-параметры совместимы с `detail_level`). Часть — **не подтвердилась**; ниже все правки.

| ID | Раздел v1 → v2 | Суть правки | Обоснование (факт кодовой базы @ `039ba7c`) |
|---|---|---|---|
| **A** | §4.2, §4.4, §7.2, §9 | `position: relative` — теперь **обязательный контракт корня каждого адаптированного Обзора**, проверяется контрактным тестом. Утверждение v1 «в подавляющем большинстве Обзоров это уже так» снято как неверное. | Слово `relative` встречается только в **2 из 54** файлов Обзор-семейства (`EdaValidationStrategyOverview.tsx`, `ModelingWorkflowOverview.tsx`). У пилотного `PreprocessingSpectralOverview.tsx` корень без позиционирования → `absolute inset-0` зацепился бы за позиционированного предка выше Обзора, и раскрытый график «уехал» бы за пределы панели. |
| **B** | §4.2, §4.3 | Из примеров кода удалены несуществующие CSS-переменные `--badge-bg`, `--badge-fg`, `--badge-bg-hover`, `--card-bg`; заменены фактическими классами проекта. | Переменных нет ни в `globals.css`, ни где-либо ещё (grep по `packages/ui` + `apps`). Реальный стиль бейджей проекта: `rounded-full bg-neutral-100 … hover:bg-neutral-200` (`PreprocessingSpectralOverview.tsx:90`). Кнопка на несуществующих переменных отрисовалась бы прозрачной. |
| **C** | §3, §4.4, §7.3 | При раскрытии корень Обзора **переключает `overflow-y-auto` → `overflow-hidden`**; при схлопывании — обратно. | Корни Обзоров скроллимые (`<section class="flex h-[468px] … overflow-y-auto">`, `PreprocessingSpectralOverview.tsx:90`). В скроллимом контейнере `absolute inset-0` привязывается к padding-box всего скроллимого содержимого: при переполнении/прокрутке раскрытая панель частично уходит из видимой области. |
| **D** | §7.1 | Клавиатурные/кликовые тесты — через `fireEvent`, зависимость `@testing-library/user-event` в v1 **не добавляется**. | `user-event` отсутствует в корневых devDependencies (есть `@testing-library/react@16.3.2`, `@testing-library/jest-dom@7`, `jest@30`). Активация `<button>` по Enter/Space — нативное поведение браузера и не подлежит тестированию в jsdom. |
| **E** | §4.2, §4.3 | Из примеров удалена утилита `cn(...)` — шаблонные строки, как в существующем коде. | `cn()` не существует в `packages/ui/lib` (10 модулей, утилиты объединения классов нет); все существующие компоненты используют шаблонные строки. |
| **F** | §7.2, §11 | Покрытие контрактного теста расширено с паттерна `*Overview.tsx` до **явного списка 54 файлов** (`*Overview` + `*Pipeline` + `*Visualizations`). | Прецедент проекта (`AnalysisWorkspaceHeight.test.ts`, `AdaptiveWorkspaceVisualizations.test.ts`) — явные списки, не глоббы. Паттерн `*Overview.tsx` пропустил бы ~24 файла, реально рендерящих графики (`PreprocessingSpectralPipeline.tsx`, `PreprocessingMissingVisualizations.tsx` и т.п.). |
| **G** | §4, §11 | Отказ от новых подкаталогов `components/charts/` и `components/__contracts__/` — плоская схема `components/`, контрактные тесты рядом с компонентами. | Фактическая структура `packages/ui/components/` — плоская (170+ файлов); существующие контрактные тесты лежат в корне каталога. Новая вложенность ломала бы сложившийся импорт-стиль без выигрыша. |
| **H** | §4.1, §9 | Разделение контекстов на **state** и **actions** (стабильные по ссылке действия) против лавины ре-рендеров. | Новое осознание: 30+ Обзоров с тяжёлыми Recharts-инстансами; в v1 смена `expandedChartId` ре-рендерила бы **все** панели Обзора, а не только затронутые. |
| **I** | §4.3, §7.1 | На кнопке-бейдже добавлен `aria-expanded`; уточнены клавиатурные проверки. | Доступность: состояние раскрытия должно быть озвучиваемым скринридерами; v1 имел только `aria-label`. |
| **J** | §7.4 | Byte-for-byte regression-тест заменён на тест **структуры и объёма** ответа с оговоркой о детерминизме. | Абсолютное побайтовое сравнение JSON хрупко: достаточно одного нестабильного поля (порядок ключей, float-форматирование, служебные поля) — тест начнёт «мигать» на CI. Контракт защиты — ключи, типы, количество точек/бинов. |

Подтверждённые предпосылки v1, которые **не менялись**: масштаб Обзоров, контракт `h-[468px]`
(51 файл), паттерн `ResponsiveContainer` (23 файла, перенос размера «бесплатно»), lucide-react
(`^0.427.0`, используется в standalone и `packages/ui/lib`), recharts `^2.13.3`, каталог
`packages/ui/hooks/` (существует, `useTargetColumn.ts`), обратная совместимость `detail_level`
для FastAPI-эндпоинтов, non-goals, single-expand модель, поэтапный роллаут.

---

## 1. Проблема

«Обзор» на всех вкладках платформы работает в фиксированном контракте высоты
рабочего окна `flex h-[468px] min-h-0 flex-col` (Task 88), внутри которого
вложенные визуализации (Recharts `ResponsiveContainer` с `height="100%"`,
Task 89) делят это пространство между собой, а иногда и с описанием,
переключателем представлений (бейджи-вкладки) и другими элементами.

При большом количестве точек/рядов/срезов (спектральный анализ CWT,
структурные сдвиги, матрица моделей, декомпозиция с 3–4 компонентами
одновременно и т.д.) плотность информации на графике внутри 468px-окна
становится недостаточной: подписи осей накладываются, легенда обрезается,
тонкие структурные детали (локальные экстремумы, узкие сдвиги, кластеры
scales на CWT-хитмапе) неразличимы аналитиком.

Факт кодовой базы: контракт `h-[468px]` используется в 51 файле,
`ResponsiveContainer width="100%" height="100%"` — в 23 файлах `packages/ui/components/`.

## 2. Цель

Дать аналитику единый, предсказуемый способ «раскрыть» любой вложенный
график из «Обзора» до размера всего окна Обзора и вернуть его обратно —
без перехода на другую страницу, без потери состояния (выбранного
представления, датасета, настроек), одним и тем же жестом на любой вкладке
платформы.

### Вне рамок задачи (non-goals)
- Полноэкранный (viewport) режим/модальное окно поверх всей страницы —
  спецификация ограничивается раскрытием **в пределах контейнера Обзора**
  (буквально «до полного размера окна Обзор», не более).
- Изменение методологии расчётов (PELT, CWT, STL и т.д.) — сэмплирование
  касается только объёма данных, отдаваемых для отрисовки, не самого
  анализа.
- Одновременное раскрытие нескольких графиков — вне скоупа v1 (см. §5.2).
- Введение дизайн-токенов (`--badge-*`, `--card-bg` и т.п.) — возможный
  отдельный cosmetic-task, здесь не требуется (см. правку B).

## 3. Пользовательский сценарий

1. Аналитик открывает любую вкладку (например, «Предобработка → Спектральный
   анализ»), видит «Обзор» с вложенным графиком CWT-хитмапа, сжатым до части
   468px-окна.
2. В правом верхнем углу графика — иконка-бейдж (в стиле принятых в проекте
   «светло-серых круглых бейджей» Task 88/89: `rounded-full bg-neutral-100`,
   см. правку B) с пиктограммой «развернуть» (две стрелки по диагонали
   наружу, Maximize2).
3. Клик по иконке → график плавно (≈200мс transition) занимает всё окно
   Обзора (468px по высоте, полная ширина панели), скрывая соседние
   элементы этого конкретного визуального блока (описание, другие
   sub-графики того же блока), но **не** ломая контракт высоты — Обзор
   не «вырастает» за пределы 468px, он просто отдаёт всё пространство
   одному графику.
4. Иконка меняется на «свернуть» (Minimize2) и остаётся на том же месте
   (правый верхний угол) поверх раскрытого графика.
5. Повторный клик по этой же иконке — или `Esc` — схлопывает график обратно
   к исходному размеру, соседние элементы возвращаются.
6. Если для раскрытого состояния предусмотрено более детальное
   сэмплирование (см. §6) — при раскрытии показывается лёгкий inline-лоадер
   поверх уже видимого (сжатого) графика, без дёргания layout, после чего
   график перерисовывается с более высоким разрешением.
7. Переключение вкладки представления внутри Обзора, смена датасета/target,
   уход со страницы — всегда сбрасывают раскрытое состояние в свёрнутое
   (защита от «залипшего» полноразмерного графика в неактуальном контексте).
8. **[Правка C]** Пока какой-либо график Обзора раскрыт, корень Обзора
   находится в режиме `overflow-hidden` (вертикальная прокрутка Обзора
   временно недоступна); после схлопывания прокрутка возвращается. Аналитик
   этого не замечает: раскрытый график занимает всё окно, скроллить
   нечему и незачем.

## 4. Архитектура компонентов (packages/ui)

Поскольку фича должна работать одинаково на **всех** вкладках и в обоих
приложениях (embedded + standalone) без дублирования кода, она реализуется
как переиспользуемый примитив в `packages/ui`, по той же логике, что и
`TsAnalysisPreprocessing` — общая бизнес/UI-логика, используемая обоими
фронтендами.

**[Правка G]** Расположение файлов — по фактически сложившейся плоской схеме
`packages/ui/components/` (как у всех 170+ существующих компонентов и обоих
контрактных тестов), без новых подкаталогов:

```
packages/ui/
  components/
    ExpandableChartPanel.tsx        — обёртка вокруг одного визуального блока
    ExpandableChartPanel.test.tsx
    ChartExpandToggle.tsx           — сама иконка-кнопка (бейдж)
    ChartExpandToggle.test.tsx
    ExpandableChartsProvider.tsx    — контекст на уровень одного «Обзора»
    ExpandableChartsProvider.test.tsx
    ExpandableChartCoverage.test.ts — контрактный тест (рядом с существующими
                                     AnalysisWorkspaceHeight.test.ts и
                                     AdaptiveWorkspaceVisualizations.test.ts)
  hooks/
    useExpandableChart.ts           — хуки доступа к state/actions-контекстам
    useChartDetailData.ts           — хук условной догрузки детального
                                      сэмплирования (см. §6, Этап 3)
```

### 4.1 `ExpandableChartsProvider`

Контекст-провайдер, инстанцируется **один раз на каждый компонент-Обзор**
(не глобально на всё приложение — раскрытие в одной вкладке не должно
влиять на состояние в другой).

**[Правка H]** Контекст разделён на два: state-контекст (значение
`expandedChartId`) и actions-контекст (стабильные по ссылке функции).
Компоненты, использующие только действия (например, кнопка «свернуть всё»
в шапке Обзора), не перерендериваются при смене раскрытого графика; тяжёлые
Recharts-поддеревья перерисовываются только если они реально зависят от
`expandedChartId` (а с условным рендером §4.4 невидимые панели вообще
размонтируются).

Состояние:
```ts
type ExpandableChartsState = {
  expandedChartId: string | null;
};
```

Инварианты:
- В любой момент времени раскрыт **не более одного** графика в пределах
  одного Обзора (простая модель, не требует z-index-стека).
- `expand(id)` — если уже что-то раскрыто, сначала неявно схлопывает
  предыдущее (single-expand guarantee), затем раскрывает новое.
- `collapseAll()` вызывается автоматически по эффектам, зависящим от
  ключей инвалидации, уже принятым в проекте для профилей (смена
  `datasetId`, `targetColumn`, переключение таба представления внутри
  Обзора, размонтирование компонента).

```tsx
const ExpandableChartsStateContext = createContext<{ expandedChartId: string | null } | null>(null);

const ExpandableChartsActionsContext = createContext<{
  expand: (id: string) => void;
  collapse: () => void;
  toggle: (id: string) => void;
} | null>(null);

export function ExpandableChartsProvider({ children }: PropsWithChildren) {
  const [expandedChartId, setExpandedChartId] = useState<string | null>(null);

  // [Правка H] Действия стабильны по ссылке: потребители actions-контекста
  // не перерендериваются при смене expandedChartId.
  const actions = useMemo(
    () => ({
      expand: (id: string) => setExpandedChartId(id),
      collapse: () => setExpandedChartId(null),
      toggle: (id: string) =>
        setExpandedChartId(prev => (prev === id ? null : id)),
    }),
    []
  );

  return (
    <ExpandableChartsActionsContext.Provider value={actions}>
      <ExpandableChartsStateContext.Provider value={{ expandedChartId }}>
        {children}
      </ExpandableChartsStateContext.Provider>
    </ExpandableChartsActionsContext.Provider>
  );
}
```

Хуки доступа (`packages/ui/hooks/useExpandableChart.ts`):

```tsx
export function useExpandableChartState() {
  const ctx = useContext(ExpandableChartsStateContext);
  if (!ctx) throw new Error("ExpandableChartsProvider is missing above this component");
  return ctx;
}

export function useExpandableChartActions() {
  const ctx = useContext(ExpandableChartsActionsContext);
  if (!ctx) throw new Error("ExpandableChartsProvider is missing above this component");
  return ctx;
}
```

### 4.2 `ExpandableChartPanel`

Оборачивает **один визуальный блок** (не весь Обзор целиком) — то есть если
на вкладке «Спектральный анализ» одновременно показаны периодограмма и
CWT-хитмап, каждый оборачивается отдельным `ExpandableChartPanel` со своим
`chartId`, и раскрывается только тот, по которому кликнули.

**[Правка A]** Раскрытие через `absolute inset-0` корректно только внутри
позиционированного контейнера. Контракт: **корень Обзора обязан иметь
класс `relative`**. Проверка кодовой базы показала, что сегодня он есть
лишь в 2 из 54 файлов Обзор-семейства, поэтому `relative` добавляется
к корню **каждого адаптируемого Обзора в его же PR** и становится
обязательным требованием контрактного теста (§7.2).

**[Правка B]** Фон раскрытой панели — `bg-white`, как у всех карточек Обзоров
(переменная `--card-bg` в проекте не существует).

**[Правка E]** Вместо несуществующей утилиты `cn(...)` — шаблонные строки
(паттерн всего существующего кода `packages/ui`).

```tsx
type ExpandableChartPanelProps = {
  chartId: string;                 // уникален в пределах Обзора, стабилен
  title?: string;                  // опционально дублируется в raised-режиме
  className?: string;
  children: ReactNode;             // сам вложенный график (Recharts и т.п.)
  onExpandChange?: (expanded: boolean) => void; // для §6-триггера дозагрузки
};

export function ExpandableChartPanel({
  chartId, title, className, children, onExpandChange,
}: ExpandableChartPanelProps) {
  const { expandedChartId } = useExpandableChartState();
  const { toggle } = useExpandableChartActions();
  const isExpanded = expandedChartId === chartId;

  useEffect(() => {
    onExpandChange?.(isExpanded);
  }, [isExpanded, onExpandChange]);

  // Esc закрывает раскрытый график
  useEffect(() => {
    if (!isExpanded) return;
    const onKey = (e: KeyboardEvent) => {
      if (e.key === "Escape") toggle(chartId);
    };
    window.addEventListener("keydown", onKey);
    return () => window.removeEventListener("keydown", onKey);
  }, [isExpanded, toggle, chartId]);

  return (
    <div
      className={`${isExpanded
        ? "absolute inset-0 z-20 flex flex-col bg-white"
        : "flex min-h-0 flex-1 flex-col"} ${className ?? ""}`}
    >
      <ChartExpandToggle
        expanded={isExpanded}
        onClick={() => toggle(chartId)}
        title={title}
      />
      <div className="min-h-0 flex-1">{children}</div>
    </div>
  );
}
```

Ключевой момент (теперь — жёсткий контракт, см. правку A): **родительский
контейнер Обзора обязан иметь `position: relative`**, чтобы `absolute
inset-0` разворачиваемого блока перекрывал именно область Обзора. Так как
фактически `relative` есть только у 2/54 Обзоров, каждый адаптируемый Обзор
получает его в собственной PR-итерации, а контрактный тест (§7.2) не даёт
зарелизить адаптацию без него.

### 4.3 `ChartExpandToggle`

**[Правка B]** Стили — фактический стиль бейджей проекта
(`rounded-full bg-neutral-100`, hover `bg-neutral-200`, фокус-ринг
`ring-neutral-400` — как у переключателей представлений, см.
`PreprocessingSpectralOverview.tsx:90`), без несуществующих CSS-переменных.
**[Правка E]** Без `cn(...)`. **[Правка I]** Добавлен `aria-expanded`.

```tsx
type ChartExpandToggleProps = {
  expanded: boolean;
  onClick: () => void;
  title?: string;
};

export function ChartExpandToggle({ expanded, onClick, title }: ChartExpandToggleProps) {
  const label = expanded ? "Свернуть график" : "Развернуть график до размера окна Обзора";
  return (
    <button
      type="button"
      onClick={onClick}
      aria-label={label}
      aria-expanded={expanded}
      title={title ?? label}
      className={
        "absolute right-2 top-2 z-30 flex h-7 w-7 items-center justify-center " +
        "rounded-full bg-neutral-100 text-neutral-600 hover:bg-neutral-200 " +
        "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-neutral-400 " +
        "transition-colors"
      }
    >
      {expanded ? <Minimize2 size={14} /> : <Maximize2 size={14} />}
    </button>
  );
}
```

Используется существующая иконочная зависимость проекта (`lucide-react`,
`^0.427.0` — уже в `packages/ui/package.json` и используется в standalone-оболочке
и `packages/ui/lib`), `Maximize2`/`Minimize2` — семантически однозначные
пиктограммы «развернуть/свернуть» (не «крестик закрытия» — сохраняем
интуитивную симметрию действия, требуемую в задаче: «эту же иконку
раскрывает и схлопывает»).

Доступность:
- `aria-label` меняется в зависимости от состояния;
- `aria-expanded` отражает состояние раскрытия (правка I);
- фокусируется табом, активируется `Enter`/`Space` (нативное поведение
  `<button>`);
- видимый `focus-visible` ринг;
- `Esc` схлопывает при раскрытом состоянии (обработка в `ExpandableChartPanel`, §4.2).

### 4.4 Интеграция в существующие Обзоры

Каждый компонент-Обзор (все 30 `*Overview.tsx` плюс `*Pipeline`/`*Visualizations`
семейство — суммарно 54 файла, см. §7.2) получает на верхнем уровне разметки:

**[Правка A]** `relative` добавляется к постоянным классам корня.
**[Правка C]** `overflow-y-auto` заменяется на условную пару: при раскрытом
графике — `overflow-hidden`, иначе — `overflow-y-auto`.

```tsx
// [Правки A+C] корень Обзора:
// relative — всегда; overflow переключается по наличию раскрытого графика
export function SpectralAnalysisOverview(props: SpectralAnalysisOverviewProps) {
  return (
    <ExpandableChartsProvider>
      <SpectralAnalysisOverviewInner {...props} />
    </ExpandableChartsProvider>
  );
}

function SpectralAnalysisOverviewInner(props: SpectralAnalysisOverviewProps) {
  const { expandedChartId } = useExpandableChartState();
  return (
    <section
      className={`relative flex h-[468px] min-h-0 flex-col rounded-lg border
        border-neutral-200 bg-white feed-scroll
        ${expandedChartId ? "overflow-hidden" : "overflow-y-auto"}`}
    >
      {/* шапка: заголовок, метрики, бейджи-вкладки представлений */}
      {/* [Правка H] при раскрытом графике бейджи-вкладки скрываются:
          useExpandableChartState() уже подписал Inner на expandedChartId */}
      {props.view === "global" && (
        <ExpandableChartPanel chartId="spectral-periodogram" title="Периодограмма">
          <ResponsiveContainer width="100%" height="100%">
            {/* существующий Recharts-график без изменений */}
          </ResponsiveContainer>
        </ExpandableChartPanel>
      )}
      {/* футер: методология, предупреждения, источники */}
    </section>
  );
}
```

Важно: **сам Recharts-график не меняется** — меняется только контейнер
вокруг него. `ResponsiveContainer height="100%"` уже подхватывает новый
раздутый размер автоматически (отработанный паттерн Task 89), поэтому
большая часть выигрыша в читаемости достаётся «бесплатно», без правок
самих графиков. Обёртка-компонент с провайдером снаружи и Inner-компонентом
внутри нужна, чтобы корень Обзора имел доступ к `expandedChartId` для
переключения overflow (правка C) и скрытия бейджей, оставаясь внутри
провайдера.

Для Обзоров с несколькими одновременными визуализациями (например, три
компоненты декомпозиции сразу) — оборачивается **каждая** компонента
своим `ExpandableChartPanel` с отдельным `chartId`; при раскрытии одной
остальные (не раскрытые) не рендерятся через условный рендер
(`expandedChartId && expandedChartId !== chartId ? null : <Panel />`) —
это перекрывает их визуально и заодно исключает CPU-расход тяжёлых
невидимых Recharts-инстансов (см. §7 — тест производительности).

## 5. Взаимодействие с уже существующими UI-паттернами Обзора

### 5.1 Переключатель представлений (бейджи-вкладки, Task 90–93)

Если внутри Обзора есть свой собственный переключатель представлений
(например, «Паспорт свойств ряда» на 3 вкладках), раскрытие графика не
подменяет и не сбрасывает выбор представления — раскрывается **текущий
активный** график внутри текущей вкладки представления. Смена вкладки
представления во время раскрытого состояния — триггер `collapse()`
(см. §3, п.7), чтобы не путать аналитика раскрытым графиком от другого
представления. При этом сами бейджи-вкладки (шапка Обзора) при раскрытом
графике скрываются — визуально Обзор полностью отдан графику (§4.4).

### 5.2 Почему не multi-expand в v1

Раскрытие сразу нескольких графиков потребовало бы либо деления окна
Обзора на несколько под-панелей (усложнение вёрстки, размывание цели
задачи — «раскрыть график до размера **всего** окна»), либо стека
z-index с ручным управлением порядком. Оставляем как явный follow-up
(см. §9), если аналитики запросят сравнение двух раскрытых графиков
бок о бок.

## 6. Сценарий сэмплирования (детализация при раскрытии)

### 6.1 Почему это нужно

Часть визуализаций уже сегодня отдаются backend'ом с зафиксированным
потолком точек/бинов/scales ради производительности и объёма payload
(например, бюджетирование PELT-сетки из Task 76, лимиты точек CWT и
т.п.). Такой потолок разумен для компактного 468px-окна, но при
раскрытии на всё окно у графика физически появляется место для
значительно большей плотности данных без потери читаемости — а
пользователь, который явно попросил «раскрыть», ожидает увидеть
**больше деталей**, а не тот же самый разреженный график, просто
растянутый крупнее.

### 6.2 Модель: два уровня детализации, один и тот же endpoint

Не создаём отдельные endpoint'ы — расширяем существующие профильные
GET/POST-запросы (spectral-profile, decomposition-profile,
structural-breaks, variance-profile, smoothing-profile,
stationarity-profile, feature-generation-profile, scaling-profile,
model-matrix и т.д.) опциональным параметром:

```
GET /v1/.../spectral-profile?session_id=...&detail_level=compact   (по умолчанию)
GET /v1/.../spectral-profile?session_id=...&detail_level=expanded
```

- `compact` — текущее поведение без изменений (обратная совместимость,
  ничего не ломается для клиентов, ещё не знающих о новом параметре).
- `expanded` — тот же самый расчёт (методология не меняется), но с более
  высоким вторичным потолком точек/бинов/scales, зафиксированным в
  конфиге каждого профиля (по аналогии с бюджетом PELT-сетки Task 76 —
  вторичный потолок тоже должен быть явным и протестированным, чтобы не
  провоцировать регресс времени ответа/памяти на render.com).

Таблица предлагаемых вторичных потолков (стартовая гипотеза, подлежит
утверждению тимлидом и калибровке на реальных датасетах):

| Профиль                     | compact (текущий)         | expanded (предлагаемый)      |
|------------------------------|----------------------------|--------------------------------|
| Спектральный (периодограмма) | текущий лимит точек        | ×2, но не более технич. потолка |
| CWT-хитмап                   | текущие scales × time      | ×1.5–2 по каждой оси            |
| Структурные сдвиги (PELT)    | текущий бюджет сетки       | расширенный бюджет (по аналогии с Task 76 budget) |
| Декомпозиция (STL и др.)     | текущее число точек ряда   | без даунсэмплинга (полный ряд, если в разумных пределах) |
| Матрица моделей / backtest   | текущее число фолдов/точек | +детализация по фолдам          |

Точные числа — предмет отдельной калибровочной задачи с бенчмарками
(см. §9, follow-up), в этой спецификации фиксируется **контракт**
(наличие параметра, обратная совместимость, вторичный потолок как явная
константа), а не конкретные цифры. Этапы 1–2 (фронтенд, см. §8) от
калибровки не зависят и могут выполняться параллельно с ней.

### 6.3 Клиентский флоу дозагрузки

```
useChartDetailData(sessionId, profileKey, params)
```

1. При `expand()` конкретного `chartId` компонент, отрисовывающий график
   (не сам `ExpandableChartPanel` — он универсален и не знает про data
   fetching), через `onExpandChange` получает сигнал «стал раскрыт».
2. Если для данного профиля кэш `detail_level=expanded` для текущего
   `(sessionId, profileKey, fingerprint)` уже есть — используется сразу,
   без похода в сеть.
3. Если кэша нет — выполняется фоновый повторный запрос с
   `detail_level=expanded`; пока он летит, **на месте графика продолжает
   отображаться уже отрисованный compact-график**, растянутый в
   увеличенный контейнер (Recharts это отрисует корректно за счёт
   `ResponsiveContainer`), поверх — лёгкий unobtrusive-индикатор
   (например, тонкий прогресс-бар сверху панели, не блокирующий
   просмотр текущих данных).
4. По готовности — плавная замена данных графика (без перезагрузки
   компонента, без сброса зума/tooltip-состояния, если такое есть).
5. Кэш детального payload живёт в рамках текущей сессии анализа и
   инвалидируется теми же триггерами, что и остальные профили
   (смена датасета/target/session — уже реализованный паттерн
   инвалидации из Task 90+).
6. **Graceful degradation:** если backend ещё не поддерживает
   `detail_level` для конкретного профиля (поэтапный роллаут, §8) —
   запрос с этим параметром либо игнорируется backend'ом (параметр
   Optional, не ломает существующий контракт), либо, если endpoint
   ещё не тронут вовсе, клиент просто не делает дозапрос и показывает
   compact-данные в увеличенном виде — это уже даёт часть пользы
   (Recharts сам лучше расставит подписи/легенду на большей площади)
   без каких-либо изменений backend.

### 6.4 Что не меняется

- Ни один существующий контракт `compact`-ответа не меняется — только
  добавляется optional query param с default, сохраняющим текущее
  поведение.
- Данные остаются read-only, никакой мутации исходного ряда/сессии.
- Методология расчётов (PELT, STL, CWT-параметры и т.д.) идентична для
  обоих уровней детализации — различается только объём **отдаваемых для
  отрисовки** точек, не сам анализ.

## 7. Тестовая стратегия (TDD, по протоколу AGENTS.md)

### 7.1 RED → GREEN, компонентный уровень

**[Правка D]** Все взаимодействия — через `fireEvent` из `@testing-library/react`
(зависимость уже в корневых devDependencies). `@testing-library/user-event`
в проекте отсутствует и в этой задаче не добавляется; активация `<button>`
по `Enter`/`Space` — нативное поведение браузера и в jsdom не тестируется.
**[Правка I]** Проверяется `aria-expanded`.

- `ExpandableChartsProvider.test.tsx`:
  - `expand(id)` устанавливает `expandedChartId`;
  - повторный `toggle(id)` того же id — схлопывает (инвариант single-expand);
  - `expand(id2)` при уже раскрытом `id1` — раскрывает только `id2`
    (implicit collapse предыдущего);
  - потребители actions-контекста не перерендериваются при смене
    `expandedChartId` (счётчик рендеров на consumer, правка H).
- `ChartExpandToggle.test.tsx`:
  - рендерит нужную иконку, `aria-label` и `aria-expanded` в зависимости
    от `expanded` (правка I);
  - клик (`fireEvent.click`) вызывает `onClick` ровно один раз;
  - фокус-стили присутствуют в className (`focus-visible:ring-2`).
- `ExpandableChartPanel.test.tsx`:
  - в свёрнутом состоянии — обычный flex-блок, без `absolute`/`z-20`;
  - в раскрытом — применяются `absolute inset-0 z-20`;
  - `Esc` при раскрытом состоянии вызывает `toggle`
    (`fireEvent.keyDown(window, { key: "Escape" })`);
  - `Esc` при свёрнутом состоянии — no-op.

### 7.2 Контрактный тест покрытия (по аналогии с Task 88/89)

`ExpandableChartCoverage.test.ts` — не рендерит компоненты, а статически
проверяет исходники (как уже принято в проекте для похожих контрактов
высоты/адаптивности). **[Правка F]** Список файлов — **явный**, по прецеденту
`AnalysisWorkspaceHeight.test.ts` / `AdaptiveWorkspaceVisualizations.test.ts`:
54 файла Обзор-семейства (30 `*Overview.tsx` + `*Pipeline.tsx` +
`*Visualizations.tsx`), состав фиксируется первым RED-прогоном. Паттерн-глобб
`*Overview.tsx` из v1 отклонён: он пропустил бы ~24 файла, реально рендерящих
графики (например, `PreprocessingSpectralPipeline.tsx`,
`PreprocessingMissingVisualizations.tsx`).

Для **каждого** файла из списка тест требует:
1. **[Правка A]** корневой контейнер содержит класс `relative`
   (и контракт высоты `h-[468px]`);
2. **[Правка C]** корневой контейнер содержит условную пару
   `overflow-hidden` / `overflow-y-auto` по `expandedChartId`;
3. внутри монтируется `ExpandableChartsProvider`;
4. каждый визуальный блок обёрнут в `ExpandableChartPanel`.

Тест изначально **RED** (ни один Обзор ещё не адаптирован) — фиксируется
полный список файлов, подлежащих доработке, что становится чек-листом
поэтапного роллаута (§8).

### 7.3 Интеграционные тесты

- Смена активного таба представления внутри Обзора при раскрытом графике
  → `expandedChartId` сбрасывается.
- Смена датасета/target при раскрытом графике → сброс.
- Размонтирование Обзора (переход на другую вкладку платформы) →
  состояние провайдера уничтожается вместе с компонентом (это происходит
  автоматически, так как провайдер живёт на уровне конкретного Обзора, а
  не глобально — отдельный тест лишь фиксирует этот инвариант дизайна).
- **[Правка C]** При раскрытии графика на корне Обзора присутствует
  `overflow-hidden` и отсутствует `overflow-y-auto`; после схлопывания —
  наоборот (проверяется по className корня).

### 7.4 Backend-тесты (при подключении `detail_level`, поэтапно)

**[Правка J]** Regression-защита `compact`-режима строится на сравнении
**структуры и объёма**, а не байтов:

- Для каждого затронутого профиля: тест, что `detail_level=compact`
  (или его отсутствие) возвращает ответ с **идентичным набором ключей,
  типов и количеством точек/бинов/scales**, что и текущий контракт
  (guard от случайного изменения дефолтного поведения). Побайтовое
  сравнение v1 заменено осознанно: JSON-сравнение хрупко к нестабильным
  полям (служебные поля, float-форматирование, порядок ключей) и будет
  «мигать» на CI; если при реализации подтвердится полная детерминированность
  конкретного профиля, для него допускается усилить тест до снапшота.
- Тест, что `detail_level=expanded` даёт **не меньше** точек/бинов, чем
  compact, и не превышает зафиксированный вторичный потолок (по аналогии
  с уже существующим тестом бюджета PELT-сетки, Task 76).
- Тест таймингов (budget test) — expanded-режим не должен провоцировать
  таймауты/502 на render.com, аналогично проблеме, исправленной в Task 76.

## 8. Поэтапный роллаут

Учитывая масштаб (все Обзоры на всех вкладках — 54 файла Обзор-семейства
по данным ревью §7.2), внедрение делится на этапы, каждый — отдельная
TDD-итерация по протоколу AGENTS.md:

1. **Этап 0 (эта спецификация).** Ревью тимлидом, утверждение вторичных
   потолков сэмплирования (§6.2), решение по открытым вопросам (§10).
2. **Этап 1 — фундамент.** Реализация `ExpandableChartsProvider` (со
   split-контекстами, правка H), `ExpandableChartPanel`, `ChartExpandToggle`,
   `useExpandableChart`, контрактный тест покрытия (RED, фиксирует полный
   список 54 файлов). Без backend-изменений — только визуальное раскрытие
   с текущими (compact) данными.
3. **Этап 2 — пилот на самых плотных визуализациях.** Подключение
   `ExpandableChartPanel` к 3–4 самым «болезненным» Обзорам, где
   проблема нечитаемости наиболее выражена (CWT-хитмап спектрального
   анализа, структурные сдвиги, матрица моделей, декомпозиция) —
   валидация UX на реальных данных перед массовым тиражированием.
   В каждой PR-итерации пилота одновременно добавляются `relative`
   (правка A) и условный overflow (правка C) — они стоят того же
   одной строки каждая и проверяются контрактным тестом.
4. **Этап 3 — сэмплирование `detail_level`.** Добавление параметра в
   backend для профилей из Этапа 2, `useChartDetailData`, тесты §7.4.
5. **Этап 4 — тиражирование.** Подключение оставшихся Обзоров по
   чек-листу из контрактного теста, до полного GREEN.
6. **Этап 5 — сэмплирование для остальных профилей** (по мере
   необходимости, не обязательно все — часть Обзоров может не выигрывать
   от detail-режима, например простые line-графики с малым числом точек;
   для них раскрытие остаётся чисто визуальным, без дозагрузки).

## 9. Риски и меры

| Риск | Мера |
|---|---|
| **[Правка A]** `absolute inset-0` без позиционированного корня: раскрытый график зацепится за предка выше Обзора и «уедет» за пределы панели. Фактически `relative` отсутствует у 52 из 54 Обзоров | `relative` — обязательный элемент адаптации каждого Обзора в его PR; контрактный тест §7.2 п.1 не пропускает файл без него |
| **[Правка C]** Скроллимый корень Обзора: `absolute inset-0` привязывается к padding-box скроллимого содержимого — при переполнении раскрытая панель частично вне видимой области | Условный overflow `overflow-hidden`/`overflow-y-auto` на корне при раскрытии; контрактный тест §7.2 п.2 + интеграционный тест §7.3 |
| Тяжёлые невидимые Recharts-инстансы (несколько графиков в одном Обзоре) продолжают рендериться под перекрытием, расходуя CPU | Условный рендер невидимых панелей (§4.4) |
| **[Правка H]** Лавина ре-рендеров: смена `expandedChartId` перерисовывает все панели Обзора с тяжёлыми Recharts | Split state/actions-контексты (§4.1); условный рендер размонтирует невидимые панели; при необходимости — `React.memo` на панели пилота (метрика — счётчик рендеров в §7.1) |
| Рост нагрузки на render.com от `detail_level=expanded` (память/время) | Явный вторичный потолок как тестируемая константа (§6.2, §7.4), budget-тест по аналогии с Task 76 |
| Обратная несовместимость для клиентов API, не знающих про `detail_level` | Параметр Optional с default = текущее поведение; regression-тест структуры/объёма (§7.4, правка J) |
| Аналитик теряет ориентацию, если раскрытие меняет масштаб/тики графика неожиданно | UX-принцип: сам график не «переизобретается» в раскрытом виде — используется тот же тип отрисовки, просто с большей площадью и (опционально) более высоким разрешением данных |
| Залипшее раскрытое состояние при смене контекста | Явные reset-триггеры, п.7 сценария (§3) и тесты §7.3 |

## 10. Открытые вопросы к тимлиду

**[К вопросам добавлены рекомендации ревьюера по итогам v2.]**

1. Утверждение конкретных вторичных потолков сэмплирования (§6.2, таблица) —
   нужны ли бенчмарки на реальных «тяжёлых» датасетах перед фиксацией чисел?
   *Рекомендация ревьюера: контракт фиксируем сейчас; числа — после
   калибровки. Этапы 1–2 от этого не зависят и идут параллельно (§6.2).*
2. Приоритет пилотных Обзоров на Этапе 2 — подтвердить список
   (CWT / структурные сдвиги / матрица моделей / декомпозиция) или
   скорректировать по фактическим жалобам аналитиков.
   *Рекомендация ревьюера: список подтверждаю; матрица моделей — кандидат
   на выбывание, если жалобы не подтверждают.*
3. Нужен ли в v1 сценарий «сравнение двух раскрытых графиков рядом»
   (см. §5.2, вынесено в follow-up) — если да, это меняет модель
   состояния провайдера (single-expand → multi-expand) и требует
   отдельного архитектурного решения.
   *Рекомендация ревьюера: нет, v1 остаётся single-expand; multi-expand —
   отдельный дизайн-документ.*
4. Показывать ли заголовок/описание графика в раскрытом состоянии
   (сейчас предполагается, что `title` в `ExpandableChartPanel`
   опционален и не обязателен к отображению — решение по UI-деталям
   лучше принимать вместе с дизайнером/тимлидом на макете).
   *Рекомендация ревьюера: показывать `title`, только если задан; финальное
   решение — по итогам Этапа 2 на реальных данных.*

## 11. Манифест файлов (к реализации на Этапе 1)

**[Правка G]** Пути скорректированы под плоскую структуру `packages/ui`
(подкаталоги `charts/` и `__contracts__/` из v1 не создаются).

Новые файлы:
- `packages/ui/hooks/useExpandableChart.ts`
- `packages/ui/hooks/useChartDetailData.ts` (Этап 3)
- `packages/ui/components/ExpandableChartsProvider.tsx`
- `packages/ui/components/ExpandableChartsProvider.test.tsx`
- `packages/ui/components/ExpandableChartPanel.tsx`
- `packages/ui/components/ExpandableChartPanel.test.tsx`
- `packages/ui/components/ChartExpandToggle.tsx`
- `packages/ui/components/ChartExpandToggle.test.tsx`
- `packages/ui/components/ExpandableChartCoverage.test.ts`

Изменяемые файлы (Этапы 2–5, поэтапно по чек-листу контрактного теста):
- 54 файла Обзор-семейства `packages/ui/components/`: 30 `*Overview.tsx`
  (Validation, Preprocessing, EDA, Modeling) + `*Pipeline.tsx` +
  `*Visualizations.tsx`. Точный список — вывод контрактного теста §7.2
  после первого RED-прогона. В каждый файл входят: `relative` на корне
  (правка A), условный overflow (правка C), обёртка в
  `ExpandableChartsProvider` + `ExpandableChartPanel`.

Backend (Этап 3, точечно):
- модули профилей, отобранные для пилота (§8, Этапы 2–3; например,
  `apps/api/preprocessing_spectral.py` и профильные маршруты
  `apps/api/routers/`) — добавление `detail_level` параметра и вторичных
  потолков.

---

*Конец спецификации v2. Отличия от v1 — в §0. Реализация — отдельными
TDD-итерациями согласно AGENTS.md, начиная с Этапа 1 после утверждения
тимлидом открытых вопросов §10.*

